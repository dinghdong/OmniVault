# OmniVault 产品需求文档 (PRD) v1.0

## 1. 项目背景与愿景
OmniVault 是一个基于 AI Agent 驱动的去中心化 VC 基金平台。传统 VC 基金 LP 无法透明了解基金运作和被投项目风险，OmniVault 通过 AI 自动化审计、流式资金拨付及链上逻辑透明化，让散户 LP 也能参与 VC 级投资，享受与机构同等的信息透明度和风控标准。

**核心理念**：OmniVault 类似现实世界的 VC 基金——LP 提供资金给基金托管，基金负责审核项目并决定投资，无需 LP 参与具体项目决策。

## 2. 核心架构与技术栈
* **区块链层**: Arbitrum (主网) / Sepolia (测试网) —— 负责资产清算与逻辑存证。
* **决策层**: OpenAI GPT-4o / Claude 3.5 —— 负责深度代码审计与财务逻辑推理。
* **预言机层**: Chainlink Functions —— 核心桥梁，负责触发链下 AI 计算并将结果回传链上。
* **收益层**: AAVE V3 (Arbitrum) —— 闲置资金自动存入 AAVE 赚取无风险收益。
* **执行环境**:
    * **MPC (Multi-Party Computation)**: 保护 OpenAI API Key。
    * **TEE (Trusted Execution Environment)**: (V2 规划) 提供硬件级隔离执行。

## 3. 核心流程设计 (V1 MVP)

### 3.1 LP 入金
1.  LP 将 ETH 存入 OmniVault Fund Vault（v1 仅支持 ETH，不接受 USDC 或其他代币）。
2.  Vault 自动将 ETH 包装为 WETH，并供应至 AAVE V3 (Arbitrum) 赚取 aWETH 基础收益。
3.  LP 获得 FundToken（代表基金份额），采用 **Rebasing** 模型——FundToken 余额（`balanceOf`）随全局收益因子（`accrualFactor`）自动复利增长，**无需 LP 主动操作**。
4.  `accrualFactor` 在项目退出产生净收益时按比例放大，所有 LP 的 FundToken 余额同步增长。

### 3.2 项目申请
1.  创业团队通过前端提交融资申请。
2.  输入参数：GitHub Commit Hash、合约地址、业务描述 API。
3.  数据进行链下预处理（压缩至 30KB 以内）。
4.  **无需 LP 参与任何操作**，项目申请对 LP 完全透明。

### 3.3 AI 自动化审计
1.  **触发**: OmniVault 检测到新申请，自动触发 Chainlink Functions。
2.  **逻辑注入**: 提示词存于链下，合约仅存储其哈希值（Commitment）用于公开验证；每次审计时从链下推送到节点。
3.  **计算**: Chainlink 节点通过 MPC 获取 API Key，调用 LLM 进行审计。
4.  **共识与评分**: 多个节点对 AI 返回的风险分数进行中位数聚合。**由于 LLM 输出具有非确定性，分数以"区间"形式上报（如 75–82 分），合约取各节点区间的中位数作为最终裁决分数。** 若节点间分差超过阈值（如 20 分），触发人工复核流程。
5.  **对抗防护**: 提示词中包含多样性测试集（同一逻辑的多种等价表达），防止项目方针对特定提示词进行专项代码优化。提示词采用轮换机制，每次审计随机抽取子集。

### 3.4 自动化投资
1.  **分数判定**: 审计分数超过准入阈值（默认为 80，可按项目类型差异化配置）自动激活投资流程。
2.  **资金来源**: 资金从 AAVE 赎回后发放给项目方。
3.  **分期放款**: 采用分期释放机制（参考 Aave/Compound 的分期解锁模型），首期释放 20%–30%，剩余部分按周/月线性解锁。
4.  **实时熔断**: AI Agent 持续监控以下可量化指标，一旦触发任一条件立即掐断后续流式拨付：
    *   **代码指标**: 编译警告数 > 0、代码复杂度（Cyclomatic Complexity）超过阈值、依赖库版本未更新超过 90 天。
    *   **链上指标**: 异常大额转账、合约 owner 权限变更、未预警的代理升级。
    *   **业务数据**: 业务 API 返回数据与审计时上报数据偏差超过 30%（如 TVL、活跃用户数）。
5.  **审计失败处理**: 若 AI 审计失败/超时，合约标记该项目审计异常，LP 无任何损失。

### 3.5 项目退出与收益分配
1.  **退出触发**: 创业团队被收购/上市/主动退出，或项目彻底失败触发核销。
2.  **正常退出**: 退出收益（含本金）返回 FundVault，若有净收益则 `accrualFactor` 放大，所有 LP 的 FundToken 余额自动增长。
3.  **亏损退出**: 若项目亏损（回款 < 本金），触发 `applyLoss` 缩小 `accrualFactor`，所有 LP 的 FundToken 余额同比例缩水。
4.  **彻底失败**: 项目方跑路或无任何回款，触发 `markWriteOff` 进行全额亏损核销，LP 按比例承担实际损失。

## 4. 功能模块详细定义

### 4.1 智能合约 (`OmniVault.sol`)
* **FundVault**: 统一资金托管，所有 LP 资金存入单一 Vault，统一存入 AAVE 赚取收益。
* **FundToken (Rebasing ERC-20)**: 代表 LP 份额，采用类 AAVE aToken 的 Rebasing 模型——余额（`balanceOf`）随 `accrualFactor` 自动复利增长，无需 LP 主动 claim。
* **InvestmentManager**: 管理投资项目，从 AAVE 赎回资金并向项目方分期拨款。
* **PromptRegistry**: 合约存储提示词的哈希值（Commitment），确保提示词未被篡改且公开可验证。
* **操作审计**: 记录每一次 AI 决策的 Hash，确保过程可追溯。

### 4.2 AI Agent 集群逻辑
* **审计 Agent**: 检查权限控制（如 `onlyOwner` 滥用）、代码重入风险、逻辑自洽性。
* **分析 Agent**: 交叉验证链上交易数据与链上可获取的指标（如 Gas 消耗模式、交易频率分布），识别刷单行为（Wash Trading）。注：社交媒体数据获取受限于 Chainlink Functions 的网络访问能力，该模块需在 V2 中通过第三方数据 API（如 CryptoFeeds）扩展。
* **风控 Agent**: 负责实时熔断决策，依据 §3.4 定义的量化指标执行。

### 4.3 LP 赎回机制
* **按余额赎回**: LP 持有的 FundToken 份额（`shares`）通过 `accrualFactor` 折算为可赎回的 ETH 数量（含累计复利）；`redeem(shares)` 销毁份额后从 AAVE 撤回 WETH、解包为 ETH 立即发送给 LP。注意：`redeem` 的入参是**原始 shares**（用 `getShares()` 获取），而非 `balanceOf` 返回的 ETH 计价余额。
* **赎回限制**: 单次赎回上限为基金总资产的 10%，防止大量挤兑；超限自动排队。
* **二级转让**: (V2 规划) 支持 LP 将 FundToken 转让给其他投资者，提升资金流动性。

### 4.4 项目质押与 Slash 机制
* **质押比例**: 项目方需缴纳质押保证金，质押比例参照审计分数动态调整（分数越高，质押比例越低）。
* **Slash 条件**: 熔断触发后，质押物被 Slash 并补偿给受损的 LP。
* **质押去向**: 质押代币暂存于合约，审计通过后按比例解锁。

## 5. 信任与安全模型
* **逻辑透明化**: 提示词哈希公开在合约中，用户可随时通过 Etherscan 查阅并验证链下提示词完整性。
* **机密管理**: 利用 Chainlink Secrets 门限加密，确保平台开发者也无法接触 API Key。
* **无风险收益**: 闲置资金默认存入 AAVE，LP 享受无风险基础收益。
* **博弈论设计**: 项目方质押机制，跑路或舞弊将导致质押物被 Slash。

## 6. 争议仲裁机制
* **判定主体**: 由链上治理投票（代币持有者）或链下仲裁委员会（多签）执行。
* **争议场景**: 包括但不限于：AI 审计结果争议、熔断触发争议、项目方舞弊争议。
* **裁决时效**: 争议提交后 48 小时内完成投票或仲裁，超时则默认维持原裁决。

## 7. 未来扩展 (V2+)
* 引入 ZK 证明保护项目方商业隐私。
* 支持投资 Autonomous AI Agents 团队。
* 引入家族办公室（Family Office）的跟投机制。
* 扩展社交媒体与业务数据源接入能力。
* 支持 LP 份额在协议内二级转让。
